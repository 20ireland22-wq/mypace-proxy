{
  "name": "mypace-proxy",
  "version": "1.0.0",
  "description": "MY PACE API proxy — forwards Whisper and GPT requests server-side",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
